% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lineage_and_graph.R
\name{run_lineage_and_graph}
\alias{run_lineage_and_graph}
\title{Run the lineage-and-graph cascade for an outputs group}
\usage{
run_lineage_and_graph(
  procedure,
  path_to_sas_entrypoint,
  group,
  outputs,
  format = "dot",
  verbose = FALSE,
  project_root = NULL
)
}
\arguments{
\item{procedure}{Character procedure name (the \verb{migration-<procedure>}
directory under \verb{procedures/}).}

\item{path_to_sas_entrypoint}{Character path to the SAS entrypoint file.}

\item{group}{Character outputs group name.}

\item{outputs}{Character vector of target output dataset names.}

\item{format}{Character output format passed to the graph stage: \code{"dot"},
\code{"txt"}, or \code{"llm"}.}

\item{verbose}{Logical enable verbose debug output in the graph stage.}

\item{project_root}{Character path to the migration-factory root. When
\code{NULL}, it is resolved from the installed package location.}
}
\value{
Integer \code{0} on success, non-zero when a stage fails or when one or
more requested outputs produced no lineage manifest.
}
\description{
Runs both stages in sequence for a procedure: first trace lineage for each
requested output (producing a per-output manifest), then build the combined
operations graph. If any requested output fails to produce a manifest, the
cascade fails loudly and the graph stage is skipped.
}
